# Унификация навигации и callback-роутинга

## ✅ Что сделано

### 1. Расширена app/ux/navigation.py
Добавлены утилиты для создания единообразных кнопок навигации:

- `build_cancel_keyboard()` - кнопка "❌ Отмена"
- `build_back_and_home_keyboard()` - две кнопки "🔙 Назад | 🏠 Главное меню"
- `add_navigation_buttons()` - универсальная функция для добавления навигации к любой клавиатуре
- `get_back_button()`, `get_home_button()`, `get_cancel_button()` - отдельные кнопки

### 2. Создана app/buttons/router_config.py
Единый источник истины для всех callback-ов бота:

- 80+ зарегистрированных маршрутов
- Организованы по категориям (меню, генерация, оплата, админка и т.д.)
- Поддержка трех типов callback: EXACT, PREFIX, PATTERN

### 3. Создан app/buttons/dispatcher.py
Центральный диспетчер для маршрутизации:

- `initialize_router(handlers)` - инициализация с обработчиками
- `route_callback()` - единая точка входа для всех callback-ов
- `get_router_stats()` - статистика обработки
- Автоматический fallback на неизвестные callback-и

## 📋 План интеграции

### Шаг 1: Создание обёрток для существующих обработчиков

В bot_kie.py сейчас есть огромный `_button_callback_impl()` со множеством if/elif.
Нужно:

1. Извлечь каждый блок обработки в отдельную функцию
2. Создать словарь handlers для передачи в dispatcher
3. Заменить `_button_callback_impl()` на вызов `route_callback()`

### Шаг 2: Постепенная миграция обработчиков

Для каждого callback-а:

```python
# БЫЛО (в _button_callback_impl):
if data == "back_to_menu":
    # 50+ строк кода
    await ensure_main_menu(...)
    return ConversationHandler.END

# СТАЛО (отдельная функция):
async def handle_back_to_menu(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработчик кнопки Главное меню"""
    query = update.callback_query
    user_id = query.from_user.id if query and query.from_user else None
    # код обработки
    await ensure_main_menu(...)
    return ConversationHandler.END
```

### Шаг 3: Добавление навигации везде

Использовать `add_navigation_buttons()` во всех местах создания клавиатур:

```python
# БЫЛО:
keyboard = [[btn1], [btn2]]
reply_markup = InlineKeyboardMarkup(keyboard)

# СТАЛО:
from app.ux.navigation import add_navigation_buttons

keyboard = [[btn1], [btn2]]
keyboard = add_navigation_buttons(
    keyboard, 
    lang=user_lang,
    back_callback="show_models",  # куда ведет "Назад"
    show_home=True  # показывать "Домой"
)
reply_markup = InlineKeyboardMarkup(keyboard)
```

### Шаг 4: Инициализация роутера при старте бота

В main() или где регистрируются handlers:

```python
from app.buttons.dispatcher import initialize_router

# Создать словарь обработчиков
handlers_map = {
    "handle_back_to_menu": handle_back_to_menu,
    "handle_show_models": handle_show_models,
    # ... все остальные
}

# Инициализировать роутер
initialize_router(handlers_map)
```

### Шаг 5: Упростить button_callback

```python
async def button_callback(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Единая точка входа для всех callback-ов"""
    query = update.callback_query
    data = query.data if query else None
    user_id = query.from_user.id if query and query.from_user else None
    user_lang = get_user_language(user_id)
    
    # Маршрутизировать через dispatcher
    from app.buttons.dispatcher import route_callback
    await route_callback(data, update, context, user_id, user_lang)
```

## 🎯 Ожидаемый результат

1. **Единая навигация**: Везде одинаковые кнопки "🔙 Назад" и "🏠 Главное меню"
2. **Централизованный роутинг**: Все callback-и идут через один dispatcher
3. **Автоматический fallback**: Неизвестные кнопки не ломают бота
4. **Чистый код**: Вместо 1000+ строк if/elif - четкая структура обработчиков
5. **Легкое добавление новых кнопок**: Добавить в router_config.py, создать обработчик, готово

## ⚠️ Важные замечания

- Миграцию лучше делать постепенно, по категориям callback-ов
- Сначала самые простые (back_to_menu, cancel), потом сложные
- Тестировать после каждой категории
- Старый код не удалять сразу, оставить как fallback на время миграции

## 📊 Текущее состояние

- ✅ Компоненты созданы (navigation, router_config, dispatcher)
- ⏳ Интеграция с bot_kie.py (в процессе)
- ⏳ Миграция обработчиков (не начата)
- ⏳ Тестирование (не начато)
