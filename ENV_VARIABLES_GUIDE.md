# 📋 ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ ДЛЯ TIMEWEB

## ✅ ОБЯЗАТЕЛЬНЫЕ (БЕЗ НИХ БОТ НЕ РАБОТАЕТ)

**Эти переменные ОБЯЗАТЕЛЬНО должны быть установлены:**

1. **TELEGRAM_BOT_TOKEN**
   - Токен Telegram бота
   - Получить у @BotFather
   - Пример: `YOUR_TELEGRAM_BOT_TOKEN`
   - ⚠️ **ОБЯЗАТЕЛЬНО!**

2. **KIE_API_KEY**
   - API ключ от KIE AI
   - Получить на https://api.kie.ai
   - Пример: `sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
   - ⚠️ **ОБЯЗАТЕЛЬНО!**

## 🟡 ОПЦИОНАЛЬНЫЕ (ИМЕЮТ ЗНАЧЕНИЯ ПО УМОЛЧАНИЮ)

**Эти переменные НЕ ОБЯЗАТЕЛЬНЫ, но можно установить:**

3. **KIE_API_URL**
   - URL API KIE AI
   - Значение по умолчанию: `https://api.kie.ai`
   - Можно НЕ устанавливать, если используете стандартный URL

4. **KIE_TIMEOUT_SECONDS**
   - Таймаут запросов к API (в секундах)
   - Значение по умолчанию: `30`
   - Можно НЕ устанавливать

5. **ADMIN_ID**
   - Ваш Telegram User ID
   - Значение по умолчанию: `6913446846`
   - Можно НЕ устанавливать, если используете стандартный ID
   - Узнать у @userinfobot

## 🟢 ДЛЯ ОПЛАТЫ (опционально)

**Эти переменные нужны только для проверки скриншотов оплаты:**

6. **PAYMENT_CARD_HOLDER**
   - Имя получателя платежа
   - Можно НЕ устанавливать, если не используете проверку оплаты

7. **PAYMENT_PHONE**
   - Номер телефона для СБП
   - Можно НЕ устанавливать, если не используете проверку оплаты

8. **PAYMENT_BANK**
   - Название банка получателя
   - Можно НЕ устанавливать, если не используете проверку оплаты

## 🔵 ДЛЯ ПОДДЕРЖКИ (опционально)

**Эти переменные нужны только для функции поддержки:**

9. **SUPPORT_TELEGRAM**
   - Контакт поддержки (Telegram username или ID)
   - Можно НЕ устанавливать

10. **SUPPORT_TEXT**
    - Текст сообщения поддержки
    - Можно НЕ устанавливать

## 📋 ЧТО УСТАНОВИТЬ В TIMEWEB

### ✅ МИНИМАЛЬНЫЙ НАБОР (только обязательное):

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
KIE_API_KEY=ваш_api_ключ
```

**Этого достаточно для работы бота!**

### 🟡 РЕКОМЕНДУЕМЫЙ НАБОР (обязательное + опциональное):

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
KIE_API_KEY=ваш_api_ключ
KIE_API_URL=https://api.kie.ai
KIE_TIMEOUT_SECONDS=30
ADMIN_ID=ваш_telegram_id
```

### 🟢 ПОЛНЫЙ НАБОР (все переменные):

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
KIE_API_KEY=ваш_api_ключ
KIE_API_URL=https://api.kie.ai
KIE_TIMEOUT_SECONDS=30
ADMIN_ID=ваш_telegram_id
PAYMENT_CARD_HOLDER=Дмитрий
PAYMENT_PHONE=89962176524
PAYMENT_BANK=Юмани
SUPPORT_TELEGRAM=@username
SUPPORT_TEXT=Если у вас возникли вопросы, напишите администратору
```

## ⚠️ ВАЖНО

**НЕ нужно устанавливать:**
- ❌ Переменные с пустыми значениями
- ❌ Переменные, которые имеют значения по умолчанию (если вас устраивают эти значения)
- ❌ Переменные для функций, которые вы не используете

**НУЖНО устанавливать:**
- ✅ Только обязательные переменные (TELEGRAM_BOT_TOKEN, KIE_API_KEY)
- ✅ Опциональные переменные, если хотите изменить значения по умолчанию
- ✅ Переменные для функций, которые вы используете (оплата, поддержка)

## 🎯 ИТОГО

**Для запуска бота достаточно:**
```
TELEGRAM_BOT_TOKEN=ваш_токен
KIE_API_KEY=ваш_ключ
```

**Остальные переменные опциональны!**

---

**Главное: установите хотя бы 2 обязательные переменные!**

