# Интеграция nano-banana-pro

## 📋 Обзор

Документация по интеграции модели `nano-banana-pro` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `nano-banana-pro` (или `google/nano-banana-pro` в каталоге)

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"nano-banana-pro"` |
| `input.prompt` | string | Текстовое описание изображения | Обязательный, максимум 10000 символов |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.image_input` | array | Входные изображения для трансформации | `[]` (пустой массив) |
| `input.aspect_ratio` | string | Соотношение сторон изображения | `"1:1"` |
| `input.resolution` | string | Разрешение выходного изображения | `"1K"` |
| `input.output_format` | string | Формат выходного изображения | `"png"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 10000 символов
- **Важно**: Не может быть пустым! Максимум 10000 символов (не 5000!)

#### `image_input`
- Тип: массив строк (array of strings)
- Обязательный: нет (опциональный!)
- Формат: `["https://example.com/image1.jpg", "https://example.com/image2.jpg"]`
- Количество: до 8 изображений
- Максимальный размер файла: 30MB
- Поддерживаемые форматы: JPEG, PNG, WebP
- Default: `[]` (пустой массив)
- **Важно**: Опциональный параметр! Если не указан, используется пустой массив!

#### `aspect_ratio`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"1:1"` - Квадрат (default)
  - `"2:3"` - Вертикальный 2:3
  - `"3:2"` - Горизонтальный 3:2
  - `"3:4"` - Вертикальный 3:4
  - `"4:3"` - Горизонтальный 4:3
  - `"4:5"` - Вертикальный 4:5
  - `"5:4"` - Горизонтальный 5:4
  - `"9:16"` - Вертикальный 9:16
  - `"16:9"` - Горизонтальный 16:9
  - `"21:9"` - Ультраширокий 21:9
  - `"auto"` - Автоматически на основе первого входного изображения
- **Важно**: Опциональный параметр! 11 значений (включая "auto")!

#### `resolution`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"1K"` - 1K разрешение (default)
  - `"2K"` - 2K разрешение
  - `"4K"` - 4K разрешение
- **Важно**: Опциональный параметр! 3 значения (не 2 как у Flux!)!

#### `output_format`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"png"` - PNG формат (default)
  - `"jpg"` - JPG формат
- **Важно**: Опциональный параметр! Только "png" или "jpg" (jpeg конвертируется в jpg)!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_nano_banana_pro(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для nano-banana-pro согласно документации API.
    """
    # Проверяем оба возможных ID модели
    if model_id not in ["nano-banana-pro", "google/nano-banana-pro"]:
        return True, None
    
    # Валидация prompt: обязательный, максимум 10000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для генерации изображения"
    
    prompt_len = len(prompt.strip())
    if prompt_len == 0:
        return False, "Поле 'prompt' не может быть пустым"
    if prompt_len > 10000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 10000)"
    
    # Валидация image_input: опциональный массив (до 8 изображений)
    image_input = normalized_input.get('image_input')
    if image_input is not None:
        # Нормализуем и проверяем
        ...
    
    # Валидация aspect_ratio, resolution, output_format (все опциональные)
    ...
    
    return True, None
```

### Нормализация параметров

Все функции нормализации специфичны для nano-banana-pro:

- `_normalize_aspect_ratio_for_nano_banana_pro()` - нормализует aspect_ratio (11 значений)
- `_normalize_resolution_for_nano_banana_pro()` - нормализует resolution (3 значения: 1K, 2K, 4K)
- `_normalize_output_format_for_nano_banana_pro()` - нормализует output_format (png, jpg)

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id in ["nano-banana-pro", "google/nano-banana-pro"]:
    if 'image_input' not in normalized_input:
        normalized_input['image_input'] = []  # Default согласно документации
    if 'aspect_ratio' not in normalized_input:
        normalized_input['aspect_ratio'] = "1:1"  # Default согласно документации
    if 'resolution' not in normalized_input:
        normalized_input['resolution'] = "1K"  # Default согласно документации
    if 'output_format' not in normalized_input:
        normalized_input['output_format'] = "png"  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "nano-banana-pro",
  "input": {
    "prompt": "Comic poster: cool banana hero in shades leaps from sci-fi pad. Six panels: 1) 4K mountain landscape, 2) banana holds page of long multilingual text with auto translation.",
    "image_input": [],
    "aspect_ratio": "1:1",
    "resolution": "1K",
    "output_format": "png"
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация prompt**: Проверяется длина (максимум 10000 символов)
2. **Валидация image_input**: Проверяется что это массив с до 8 валидными URL (опциональный!)
3. **Валидация aspect_ratio**: Проверяется что значение из допустимого списка (11 значений, включая "auto") (опциональный!)
4. **Валидация resolution**: Проверяется что значение "1K", "2K" или "4K" (опциональный!)
5. **Валидация output_format**: Проверяется что значение "png" или "jpg" (опциональный!)
6. **Дефолтные значения**: Применяются если параметры не указаны (image_input=[], aspect_ratio="1:1", resolution="1K", output_format="png")
7. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=nano-banana-pro TYPE=t2i INPUT_KEYS=['prompt', 'image_input', 'aspect_ratio', 'resolution', 'output_format'] 
INPUT_PREVIEW={'prompt': 'Comic poster...', 'image_input': [], 'aspect_ratio': '1:1', 'resolution': '1K', 'output_format': 'png'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt максимум 10000 символов** - проверяется при валидации (не 5000!)
2. **image_input опциональный** - не обязательный! Если не указан, используется пустой массив
3. **aspect_ratio опциональный** - не обязательный! 11 значений (включая "auto")!
4. **resolution опциональный** - не обязательный! 3 значения (1K, 2K, 4K) - не 2 как у Flux!
5. **output_format опциональный** - не обязательный! Только "png" или "jpg"
6. **Дефолтные значения**: image_input=[], aspect_ratio="1:1", resolution="1K", output_format="png"
7. **Callback URL опционален** - если не указан, уведомления не отправляются
8. **ID модели**: Поддерживаются оба варианта: "nano-banana-pro" и "google/nano-banana-pro"

## 🔄 Сравнение с другими моделями

### Сравнение с flux-2/pro-text-to-image

| Параметр | flux-2/pro-text-to-image | nano-banana-pro |
|----------|---------------------------|-----------------|
| `prompt` | От 3 до 5000 символов | Максимум 10000 символов |
| `image_input` | Не требуется | Опциональный массив (до 8) |
| `aspect_ratio` | Обязательный, 8 значений | Опциональный, 11 значений |
| `resolution` | Обязательный, "1K"\|"2K" | Опциональный, "1K"\|"2K"\|"4K" |
| `output_format` | Не требуется | Опциональный, "png"\|"jpg" |
| Тип модели | t2i | t2i |

### Сравнение с z-image

| Параметр | z-image | nano-banana-pro |
|----------|---------|-----------------|
| `prompt` | Максимум 1000 символов | Максимум 10000 символов |
| `image_input` | Не требуется | Опциональный массив (до 8) |
| `aspect_ratio` | Обязательный, 5 значений | Опциональный, 11 значений |
| `resolution` | Не требуется | Опциональный, "1K"\|"2K"\|"4K" |
| `output_format` | Не требуется | Опциональный, "png"\|"jpg" |
| Тип модели | t2i | t2i |

## 🎯 Ключевые отличия

1. **Большой prompt**: Максимум 10000 символов (не 5000!)
2. **Опциональные параметры**: Все параметры кроме `prompt` опциональны!
3. **image_input опциональный**: В отличие от других моделей, может быть пустым массивом
4. **resolution с 4K**: Поддерживает "4K" в дополнение к "1K" и "2K"
5. **output_format**: Единственная модель с параметром формата вывода
6. **11 значений aspect_ratio**: Больше вариантов, чем у других моделей
7. **Дефолтные значения**: Все опциональные параметры имеют дефолты

