# Интеграция bytedance/v1-pro-fast-image-to-video

## 📋 Обзор

Документация по интеграции модели `bytedance/v1-pro-fast-image-to-video` согласно официальной документации KIE AI API.

## 🔗 API Документация

- **URL создания задачи**: `POST https://api.kie.ai/api/v1/jobs/createTask`
- **URL статуса задачи**: `GET https://api.kie.ai/api/v1/jobs/recordInfo?taskId={taskId}`
- **Модель**: `bytedance/v1-pro-fast-image-to-video`

## 📝 Параметры запроса

### Обязательные параметры

| Параметр | Тип | Описание | Ограничения |
|----------|-----|----------|-------------|
| `model` | string | ID модели | `"bytedance/v1-pro-fast-image-to-video"` |
| `input.prompt` | string | Текстовое описание видео | Обязательный, максимум 10000 символов |
| `input.image_url` | string | URL изображения для генерации видео | Обязательный (строка, не массив!) |

### Опциональные параметры

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `input.resolution` | string | Разрешение видео | `"720p"` |
| `input.duration` | string | Длительность видео в секундах | `"5"` |
| `callBackUrl` | string | URL для уведомлений о завершении | Не указан (нет callback) |

### Допустимые значения

#### `prompt`
- Тип: string
- Обязательный: да
- Максимум: 10000 символов
- **Важно**: Не может быть пустым! Максимум 10000 символов (не 1000 или 5000!)

#### `image_url`
- Тип: string (не массив!)
- Обязательный: да
- Формат: `"https://example.com/image.jpg"`
- Максимальный размер файла: 10MB
- Поддерживаемые форматы: JPEG, PNG, WebP
- **Важно**: Это строка, не массив! Используется `image_url`, а не `image_urls`!

#### `resolution`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"720p"` - 720p разрешение (default)
  - `"1080p"` - 1080p разрешение
- **Важно**: Опциональный параметр! Только "720p" и "1080p" (не "480p"!)!

#### `duration`
- Тип: string
- Обязательный: нет (опциональный!)
- Значения:
  - `"5"` - 5 секунд (default)
  - `"10"` - 10 секунд
- **Важно**: Опциональный параметр! Только "5" и "10"!

## 🔧 Реализация в коде

### Валидация параметров

Валидация реализована в `app/services/kie_input_builder.py`:

```python
def _validate_bytedance_v1_pro_fast_image_to_video(
    model_id: str,
    normalized_input: Dict[str, Any]
) -> Tuple[bool, Optional[str]]:
    """
    Специфичная валидация для bytedance/v1-pro-fast-image-to-video согласно документации API.
    """
    if model_id != "bytedance/v1-pro-fast-image-to-video":
        return True, None
    
    # Валидация prompt: обязательный, максимум 10000 символов
    prompt = normalized_input.get('prompt')
    if not prompt:
        return False, "Поле 'prompt' обязательно для генерации видео"
    
    prompt_len = len(prompt.strip())
    if prompt_len == 0:
        return False, "Поле 'prompt' не может быть пустым"
    if prompt_len > 10000:
        return False, f"Поле 'prompt' слишком длинное: {prompt_len} символов (максимум 10000)"
    
    # Валидация image_url: обязательный (строка, не массив!)
    # ВАЖНО: Для v1-pro-fast используется image_url (строка), а не image_urls (массив)!
    image_url = normalized_input.get('image_url')
    if not image_url:
        return False, "Поле 'image_url' обязательно для генерации видео из изображения"
    
    # Проверяем что это строка
    if not isinstance(image_url, str):
        image_url = str(image_url)
    
    image_url = image_url.strip()
    if not image_url:
        return False, "Поле 'image_url' не может быть пустым"
    
    # Проверяем что URL начинается с http:// или https://
    if not (image_url.startswith('http://') or image_url.startswith('https://')):
        return False, "Поле 'image_url' должно начинаться с http:// или https://"
    
    # Сохраняем нормализованное значение
    normalized_input['image_url'] = image_url
    
    # Валидация resolution и duration (опциональные)
    ...
    
    return True, None
```

### Нормализация resolution

Функция нормализации конвертирует в нижний регистр и проверяет валидность:

```python
def _normalize_resolution_for_v1_pro_fast(value: Any) -> Optional[str]:
    """
    Нормализует resolution для bytedance/v1-pro-fast-image-to-video.
    ВАЖНО: Для v1-pro-fast поддерживаются только "720p" и "1080p" (не "480p"!)!
    """
    valid_values = ["720p", "1080p"]
    # Поддерживает алиасы (720, hd, 1080, full hd, fhd)
    ...
```

### Нормализация duration

Функция нормализации конвертирует числа в строки:

```python
def _normalize_duration_for_v1_pro_fast(value: Any) -> Optional[str]:
    """
    Нормализует duration для bytedance/v1-pro-fast-image-to-video.
    ВАЖНО: Для v1-pro-fast поддерживаются только "5" и "10"!
    """
    # Убирает "s" или "seconds" в конце
    # Конвертирует числа в строки
    ...
```

### Дефолтные значения

Если параметры не указаны, применяются дефолты:

```python
if model_id == "bytedance/v1-pro-fast-image-to-video":
    if 'resolution' not in normalized_input:
        normalized_input['resolution'] = "720p"  # Default согласно документации
    if 'duration' not in normalized_input:
        normalized_input['duration'] = "5"  # Default согласно документации
```

### Callback URL

Callback URL поддерживается через переменную окружения `KIE_CALLBACK_URL`:

```python
def get_callback_url() -> Optional[str]:
    """
    Получает callback URL из настроек.
    """
    settings = get_settings()
    callback_url = getattr(settings, 'kie_callback_url', None)
    if not callback_url:
        import os
        callback_url = os.getenv('KIE_CALLBACK_URL')
    return callback_url
```

## 📊 Пример использования

### Пример запроса

```json
{
  "model": "bytedance/v1-pro-fast-image-to-video",
  "input": {
    "prompt": "A cinematic close-up sequence of a single elegant ceramic coffee cup with saucer on a rustic wooden table near a sunlit window, hot rich espresso poured in a thin golden stream from above.",
    "image_url": "https://file.aiquickdraw.com/custom-page/akr/section-images/1762340693669m6sey187.webp",
    "resolution": "720p",
    "duration": "5"
  },
  "callBackUrl": "https://your-domain.com/api/callback"
}
```

### Пример ответа

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "281e5b0*********************f39b9"
  }
}
```

## ✅ Проверка интеграции

1. **Валидация prompt**: Проверяется длина (максимум 10000 символов)
2. **Валидация image_url**: Проверяется что это строка (не массив!) с валидным URL
3. **Нормализация image_url**: Автоматически конвертирует массив в строку (берет первый элемент)
4. **Валидация resolution**: Проверяется что значение "720p" или "1080p" (опциональный!)
5. **Валидация duration**: Проверяется что значение "5" или "10" (опциональный!)
6. **Дефолтные значения**: Применяются если параметры не указаны (resolution="720p", duration="5")
7. **Callback URL**: Передаётся в API если настроен

## 🔍 Логирование

Все параметры логируются (без секретов):

```
MODEL=bytedance/v1-pro-fast-image-to-video TYPE=i2v INPUT_KEYS=['prompt', 'image_url', 'resolution', 'duration'] 
INPUT_PREVIEW={'prompt': 'A cinematic close-up...', 'image_url': 'https://...', 'resolution': '720p', 'duration': '5'}
```

## 📚 Связанные файлы

- `app/services/kie_input_builder.py` - Валидация и нормализация параметров
- `app/kie_catalog/input_schemas.py` - Схемы входных параметров
- `app/integrations/kie_client.py` - KIE API клиент
- `app/kie_catalog/models_pricing.yaml` - Каталог моделей и цен

## ⚠️ Важные замечания

1. **prompt максимум 10000 символов** - проверяется при валидации (не 1000 или 5000!)
2. **image_url (строка, не массив!)** - Для v1-pro-fast используется `image_url` (строка), а не `image_urls` (массив)!
3. **resolution опциональный** - не обязательный! Только "720p" и "1080p" (не "480p"!)
4. **duration опциональный** - не обязательный! Только "5" и "10"
5. **Дефолтные значения**: resolution="720p", duration="5"
6. **Callback URL опционален** - если не указан, уведомления не отправляются

## 🔄 Сравнение с другими моделями

### Сравнение с wan/2-6-image-to-video

| Параметр | wan/2-6-image-to-video | bytedance/v1-pro-fast-image-to-video |
|----------|------------------------|--------------------------------------|
| `prompt` | 2-5000 символов | Максимум 10000 символов |
| `image_urls` | Обязательный массив | Не используется |
| `image_url` | Не используется | Обязательный (строка!) |
| `resolution` | Обязательный, "720p"\|"1080p" | Опциональный, "720p"\|"1080p" |
| `duration` | Обязательный, "5"\|"10"\|"15" | Опциональный, "5"\|"10" |
| Тип модели | i2v | i2v |

### Сравнение с kling-2.6/image-to-video

| Параметр | kling-2.6/image-to-video | bytedance/v1-pro-fast-image-to-video |
|----------|--------------------------|--------------------------------------|
| `prompt` | Максимум 1000 символов | Максимум 10000 символов |
| `image_urls` | Обязательный массив | Не используется |
| `image_url` | Не используется | Обязательный (строка!) |
| `sound` | Обязательный boolean | Не требуется |
| `duration` | Обязательный, "5"\|"10" | Опциональный, "5"\|"10" |
| `resolution` | Не требуется | Опциональный, "720p"\|"1080p" |
| Тип модели | i2v | i2v |

## 🎯 Ключевые отличия

1. **image_url (строка)**: В отличие от других моделей, использует `image_url` (строка), а не `image_urls` (массив)
2. **Большой prompt**: Максимум 10000 символов (не 1000 или 5000)
3. **Опциональные параметры**: `resolution` и `duration` опциональны (не обязательны!)
4. **resolution без 480p**: Только "720p" и "1080p" (не "480p"!)
5. **duration только 5 и 10**: Не поддерживает "15"
6. **Дефолтные значения**: resolution="720p", duration="5"

