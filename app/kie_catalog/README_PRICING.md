# KIE AI Models Catalog - Source of Truth

## 📋 Описание

`data/kie_pricing_rub.yaml` — единый источник правды (Source of Truth) для всех моделей KIE AI, их режимов генерации и цен в рублях.

## 📊 Источник данных

Данные синхронизируются с актуальным прайс-листом (RUB) и хранятся в репозитории как SSOT.

## 💰 Формула ценообразования

```
Цена берется напрямую из прайс-файла в рублях. Никакие курсы, множители или авто-расчеты не применяются.

## 📁 Структура файла

```yaml
version: "1.0"
currency: "RUB"
source: "KIE PRICING (RUB) — provided list"
models:
  - id: model_id              # ID модели для KIE API
    skus:
      - unit: image            # Единица: image, video, second, minute, 1000_chars, request, megapixel, removal, upscale
        price_rub: 5.11        # Точная цена в рублях
        params:               # Параметры/режимы (точное соответствие)
          quality: basic
        notes: "Basic"
```

## 🔄 Обновление каталога

Для обновления каталога:

1. Обновите данные в `data/kie_pricing_rub.yaml`
2. Запустите аудит покрытия:
   ```bash
   python scripts/pricing_audit.py
   ```

## 📝 Использование в коде

```python
from app.kie_catalog import load_catalog, get_model
from app.pricing.price_resolver import resolve_price_quote

# Загрузить каталог
catalog = load_catalog()

# Получить модель
model = get_model("flux-2/pro-text-to-image")

# Рассчитать цену для конкретных параметров
quote = resolve_price_quote(
    "flux-2/pro-text-to-image",
    mode_index=0,
    gen_type="text-to-image",
    selected_params={"resolution": "1K"},
)
```

## ⚠️ Важно

- Этот файл — **единственный источник правды** для цен
- Никаких производных расчетов или хардкода цен в коде
- Все изменения цен должны вноситься в этот файл

## 📊 Статистика

См. актуальные цифры в `PRICING_COVERAGE.md`.
